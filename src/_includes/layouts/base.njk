<!DOCTYPE html>
<html lang="en">
  {% include "components/global/head.njk" %}
  <body
    class="antialiased page--{% if page.fileSlug %}{{ page.fileSlug }}{% else %}home{% endif %} border-2 border-purple-600 md:border-red-500 lg:border-green-500 xl:border-blue-500">
    <!-- page wrapper -->
    <div class="mil-page-wrapper">
      {% include "components/global/header.njk" %}
      <div id="swupMain" class="transition-fade">
        {% block content %}
          {{ content | safe }}
        {% endblock %}
        {% include "components/global/footer.njk" %}
      </div>
    </div>
    {% include "components/global/scripts.njk" %}
  </body>
</html>